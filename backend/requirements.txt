# ─────────────────────────────────────────────────────
# QA Nexus Backend — Python Dependencies
# Install:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────

# ── Web Framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18          # Form data / file uploads

# ── Data Validation / Settings ──
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# ── Database (async SQLAlchemy + SQLite) ──
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0

# ── HTTP Client (Jira, GitHub, Slack integrations) ──
httpx>=0.28.0

# ── LLM Provider SDKs ──
openai>=1.60.0
anthropic>=0.42.0
ollama>=0.4.0
groq>=0.15.0
google-genai>=1.0.0
boto3>=1.35.0

# ── Scheduling ──
apscheduler>=3.11.0

# ── Logging ──
structlog>=24.4.0

# ── Utilities ──
uuid6>=2024.7.10
